start <- sums NEWLINE ENDMARKER
sums <- sum (NEWLINE sums)?
sum <- term '+' sum / term
term <- factor '*' term / factor
factor <- pair / group / NAME / STRING / NUMBER
pair <- '(' sum ',' sum ')'
group <- '(' sum ')'
